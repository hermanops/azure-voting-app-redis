pipeline {
  agent any

  stages {
    stage('Verify Branch') {
      steps {
        echo "$GIT_BRANCH"
      }
    }
    stage('Echo die shit!')
      steps {
        sh(script: 'echo hello World')
      }
    }
  }
}
